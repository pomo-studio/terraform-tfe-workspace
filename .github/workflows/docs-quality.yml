name: Docs quality

on:
  pull_request:
    paths:
      - '**.md'
  push:
    branches: [main]
    paths:
      - '**.md'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  docs:
    uses: pomo-studio/.github/.github/workflows/docs-quality.yml@main
    with:
      strict: true
